version: '3'
services:
  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
  db:
    image: postgres:latest
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: QWEasd123!
      POSTGRES_DB: librarydb
    ports:
      - "5434:5432"